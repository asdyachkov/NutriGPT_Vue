# Assets Images Directory

Эта папка зарезервирована для пользовательских изображений (например, загруженных рецептов, аватаров).

**Все декоративные фоны заменены на CSS-градиенты и SVG, а фото блюд берутся динамически из Pexels API** (см. `backend/internal/service/ai/image_service.go`).
